CREATE TABLE list_mangas(nome VARCHAR(30) NOT NULL, cod VARCHAR(40) NOT NULL,
                    title VARCHAR(60) NOT NULL, autor VARCHAR(30) NOT NULL,
                    arte VARCHAR(30) NOT NULL, ano int NOT NULL);